# Divisible-Room-Contracts — SIMPL

S+ sources match Divisible-Room. This fork has **no** numbered-join `.smw` yet.

1. Generate `contracts/divisible-room.chd` (`npm run generate:contract`).
2. New SIMPL program, HTML5 XPanels, **Manage GUI Extenders** → that `.chd`.
3. Drop Logic (one), Identity (per panel), Power Shutdown Confirmation (per panel).
4. Wire extender signals per [`docs/simpl.md`](../docs/simpl.md).
